# 2026-09-04 Daily Log（Day 5 — M1.5 Tool Calling）

## 今日唯一重点

模型提出 tool call ≠ 已执行；把 tool call 当 untrusted input，本地校验后再跑纯函数。

## 计划输出

- [x] 概念步：提出 vs 执行、selection / invocation / execution（用户复述确认）
- [x] executor + fixtures + runner（t1–t5）
- [x] 场景矩阵实测（5/5）
- [x] M1.5 Evaluation Cases 归档（累计 27）；用户解释确认，M1.5 Completed

## 实际完成

- 实现完成：`executor.py`（allowlist / denylist / 参数校验 / 执行计数 / tool message）；`fixtures.py`；`run_tool_scenarios.py`。
- 实测：t1 add 执行 1 次 result=5；t2 unknown / t3 invalid args / t4 denied 均 calls_executed=0；t5 role=tool 回填并字节确定序列化。

## 证据

- 代码/文档：`playground/agent-lab/m1-5-tool-calling/`；`90-days/daily/day-05-tool-calling-plan.md`
- 执行命令：`python3 run_tool_scenarios.py` → `PASS: 5 cases`
- 测试/指标：拒绝路径 calls_executed=0；`eval_cases.jsonl` 累计 27 条

## 学到的内容

### 已验证

- 模型提出 ≠ 执行：只有 selection + invocation validation 通过才进入函数。
- unknown / denied / invalid args 三条拒绝路径均有零执行证据。
- tool result 通过 role=tool + tool_call_id 显式回填下一轮。
- 用户复述确认（2026-09-04）：unknown vs denied 恢复策略不同；参数非法时函数未进入；创建 tool message 后仍须发起下一轮请求。

### 推断或待验证

- 真正的 Tool execution exception、timeout 与多工具 dispatch 留给 M1.6。

## 阻塞与失败

- 无。

## 下一步

Day 5 关闭：M1.5 Completed（用户复述确认，2026-09-04）。下次会话：M1.6 Tool Registry / Dispatch。
