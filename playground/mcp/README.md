# MCP Playground

本目录用于 Model Context Protocol 的小型互操作实验：client、server、tools、resources、prompts、transport、capability discovery 和权限边界。

实验应明确 MCP 实现/版本、client 与 server 组合、transport、暴露能力以及信任边界。默认只连接本地、低风险资源；涉及文件写入、凭据或远端副作用时加入审批与最小权限。

从 [`../_template/`](../_template/) 复制新实验。
