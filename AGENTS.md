# AGENTS.md

本文件是 `~/ai-lab` 的**最高级项目约束**，适用于所有人类贡献者和 Coding Agents。目标不是堆积代码或追逐框架，而是持续产出**可理解、可复现、可验证、可展示**的 AI Agent Engineering 成果。

## 最高级约束与当前阶段闸门

- 在本仓库的项目规则层级中，本文件具有最高优先级。任何子目录 `AGENTS.md` 只能增加更具体或更严格的局部要求，不得覆盖、削弱或绕过本文件。
- 所有 Coding Agent 在规划任务、执行命令或修改文件前，都必须先完整阅读本文件；不能以工具默认行为、框架惯例或已有模板代替这些约束。
- **当前阶段仅允许建设工作区治理、学习规划、需求分析、架构说明、文档和模板。未经用户明确授权开始实现前，不得编写业务代码、生成框架脚手架、安装项目依赖、调用外部模型/API 或创建可运行的 Agent 实现。**
- 如果任务边界不清楚，且可能越过上述阶段闸门，必须先停止并请求用户确认，不得自行推断授权。

## 1. 项目使命、能力目标与非目标

### 1.1 总体使命

- 系统学习和实践 **AI Agent Engineering**，而不是只学习 Pi 或任何单一 Agent Harness、框架、厂商 SDK。
- 将学习计划、长期知识、可控实验、工程实践和作品项目连成一条可追溯的成长链路。
- 重点培养三类相互支撑的能力：
  - **AI Agent Engineering**：agent loop、tools、state、memory、planning、MCP、evaluation 与 safety。
  - **AI Application Engineering**：LLM integration、structured output、RAG、context engineering、产品体验与质量评测。
  - **AI-Native Backend Engineering**：流式与异步执行、持久化、并发、可观测性、可靠性、成本、权限和安全。
- 优先级依次为：事实与证据、可复现性、正确性、清晰度、可维护性，最后才是速度和文件数量。

### 1.2 必须覆盖的核心机制

学习路线和实验应逐步覆盖：

- LLM API
- Structured Output
- Tool Calling
- Agent Loop
- MCP
- RAG
- Memory
- Context Engineering
- Evaluation
- Safety

对每个主题，不只记录“如何调用”，还要在实现前或实现过程中解释协议或数据流、状态边界、设计权衡、失败模式、可观测方式和评测方法。会使用某个框架 API 不等于掌握该机制。核心学习任务不得一次性生成大而全的实现并据此宣布主题完成；默认闭环是：**解释机制 → 定义最小实验 → 实现 → 运行 → 观察 → 解释结果 → 迭代**。模块完成时，用户应能用自己的话解释关键机制、边界和实验结果。

### 1.3 底层优先，而非框架优先

- 优先使用原生 HTTP、官方 SDK、标准库或职责单一的小型依赖理解和实现关键机制。
- 在引入高层抽象前，应尽量亲自实现或清楚拆解：请求/响应、schema 校验、tool dispatch、agent loop、状态转换、重试、超时、错误恢复和 tracing。
- 早期不得默认引入 LangChain、LlamaIndex 或类似大型 Agent Framework。只有在已有无框架 baseline、需求明确且收益可验证时，才可将其作为实现选项或对照实验。
- 引入大型框架时必须记录：解决的问题、替代方案、抽象代价、锁定风险、评测结果和退出路径。

### 1.4 Agent Harness 中立

- Pi 是当前主要 Coding Agent，但不是项目运行时依赖，也不是知识和决策的唯一载体。
- 仓库内受版本控制的 `AGENTS.md`、README、代码、测试、配置和决策记录是事实来源；不得依赖 Pi 会话、隐藏上下文、专属扩展或本机状态才能继续工作。
- 命令、文档和任务状态应采用通用格式，使其他 Coding Agent 或人类开发者能在新会话、甚至不同 Harness 中接手。
- Pi 专属能力只能作为可选效率增强，不得成为构建、运行、测试、评测或维护项目的必要条件。

### 1.5 产出标准与非目标

- 最终目标是形成可展示的工程作品：解决明确用户问题，能够独立安装、运行、测试、评测和演示，并说明架构、安全边界、失败案例与已知限制。
- 教程 Demo、API hello-world 和一次性 spike 只能作为学习过程中的临时产物，不能冒充完成的作品项目。
- 不以文件数量、课程打卡、框架清单、prompt 收藏或未经验证的模型输出衡量进展。
- 明确区分事实、推断、假设和待验证问题；如实记录失败实验与限制，不包装结论。
- 优先完成最小闭环，再逐步扩展；不要为尚未出现的需求提前搭建复杂平台。

### 1.6 学习模式与交付模式

- `90-days/` 与 `playground/` 默认采用 **Learning Mode**：理解优先于实现速度，核心 Agent 机制必须在实现前或实现过程中解释清楚。
- `projects/` 在用户明确要求进入交付阶段时可采用 **Delivery Mode**，此时工程效率可以获得更高优先级。
- 模式应服从任务目标；不得把每个普通工程任务都改造成教程，且 Delivery Mode 不削弱验证、安全和可复现性要求。

## 2. 指令范围与开始工作前的检查

- 本文件作用于整个工作区。子目录若存在更深层的 `AGENTS.md`，其规则只在对应子树内补充或细化本文件；发生冲突时以根 `AGENTS.md` 为准。
- 开始修改前，先阅读：根 `README.md`、本文件、目标目录最近的 `README.md` / `AGENTS.md`，以及将被修改的现有代码和测试。
- 用户在当前任务中的明确要求优先；若它与安全、成本或可复现性要求有冲突，应指出风险并采用最保守的可行方案。
- 除非用户明确要求，不修改 `~/ai-lab` 之外的文件，不顺手重构无关代码，不覆盖他人的未完成工作。

## 3. 目录边界

| 目录 | 用途 | 不应放入 |
|---|---|---|
| `90-days/` | 有时限的学习路线、每日记录、阶段复盘 | 产品源码、通用知识正文 |
| `knowledge/` | 经过提炼的长期知识、概念模型、资料索引 | 原始聊天转储、未经验证的零散输出 |
| `playground/` | 围绕单一问题的短小实验和技术 spike | 需要长期维护的产品、跨实验共享的隐式依赖 |
| `projects/` | 可运行、可测试、可演示的长期作品 | 一次性草稿、没有说明的代码片段 |
| `templates/` | 可复制的文档模板 | 运行时代码和项目公共库 |

- 不要让 `projects/` 通过相对路径直接依赖 `playground/`。实验升级为项目时，应提取或重新实现必要代码，并补齐文档与测试。
- 不要为了“看起来完整”创建大量空目录；只有出现真实内容时再扩展层级。
- 每个实验和项目保持依赖、配置和运行入口相对独立；除非已有两个以上稳定消费者，不提前创建全局 `common/` 或 `utils/`。

## 4. 标准工作流

1. **定位**：确认产物应进入哪个目录，并检查是否已有相近内容。
2. **定义**：写清目标；实验需写假设和成功指标，项目任务需写验收条件。
3. **最小实现**：先完成最小、可验证的改动，避免无关重构和批量格式化。
4. **验证**：运行与改动最接近的测试、lint、类型检查或最小 smoke test。
5. **记录**：更新对应 README、索引、实验结论或决策记录；保留失败和限制。
6. **交付**：说明变更文件、实际执行的验证、未解决问题和建议的下一步。

## 5. 可运行单元与可复现性

任何新建的可运行实验或项目至少应具备：

- 一个 README，包含目标、状态、前置条件、安装、运行、验证、已知限制和下一步。
- 明确的依赖清单；依赖管理器支持时提交 lockfile。不要依赖未记录的全局软件包。
- 若使用环境变量，提交 `.env.example`，只写变量名和安全示例，不写真实凭据。
- 命令应能从该实验或项目根目录执行；不能执行时要记录准确原因。
- 涉及模型时，记录 provider、模型标识、关键参数、prompt/config 版本；避免把这些值散落硬编码在业务逻辑中。
- 涉及随机性时尽可能固定 seed；记录重要的运行环境、数据版本和评测时间。
- 大型模型、私有数据、缓存和生成物不纳入版本控制；在 README 中说明获取或重建方式。

项目依赖应安装在项目自己的虚拟环境或包管理环境中。不要为了单个实验污染系统级运行时。

## 6. 实验与评测纪律

- 每个实验都应回答一个清晰问题，并记录：假设、基线、变量、数据、指标、结果、结论与下一步。
- 比较方案时保持数据集、采样参数、预算和评测条件一致；不使用挑选出来的少数示例代替评测。
- 没有可比基线和测量结果时，不宣称“更好”“更快”或“更智能”。
- 将框架自身的日志与真正的质量指标区分开；同时关注任务成功率、延迟、token/费用和失败模式。
- 重要结果应带日期并保留历史；不要静默覆盖不利结果。小型结果摘要可以入库，大型原始输出应外置并记录位置。
- 优先使用离线 fixture、mock 或小数据集完成开发；调用真实外部服务前先验证本地路径。

## 7. 技术路线、代码与设计规则

- 在用户授权进入实现阶段后，仍应从最小、透明的底层实现开始；LLM 请求、schema 校验、tool dispatch、循环终止条件、状态变化和错误路径不得被无法解释的框架魔法遮蔽。
- 选择满足当前目标的最简单设计；函数和模块职责单一，边界明确。
- 配置放在边界层，不把密钥、机器路径、模型名称或环境特有值硬编码进核心逻辑。
- 对关键行为和已修复 bug 添加测试；外部 API 默认可替换、可 mock，并优先提供离线验证路径。
- 代码、测试和操作命令必须独立于 Coding Agent Harness；不得要求 Pi 或其他 Harness 才能执行。
- 注释解释“为什么”和约束，不复述代码；删除死代码，不保留大段注释掉的实现。
- 尊重项目已有语言、工具链和风格。新增依赖前先判断标准库或现有依赖是否足够，并说明引入理由。
- 目录和普通文档文件名默认使用 `kebab-case`；语言要求的包/模块命名遵循该语言惯例；日期统一使用 `YYYY-MM-DD`。

## 8. 文档与知识沉淀

- 默认使用中文撰写说明、学习记录、复盘和面向用户的文本；代码标识符、CLI、配置键和行业通用术语可保留英文。
- `90-days/ROADMAP.md` 定义计划学习内容；`90-days/PROGRESS.md` 记录实际完成工作、证据、未解决问题和下一步，计划与事实不得混写。
- 知识笔记应尽量原子化，包含来源、适用边界和验证状态；明确标注自己的推断或尚未验证的内容。
- 引用外部资料时给出标题与 URL，少量引用并自行总结，不整页复制厂商文档。
- 新增长期知识时更新 `knowledge/INDEX.md`；新增项目时更新 `projects/README.md`；调整学习计划时同步 `90-days/`。
- 使用相对链接，避免写死本机绝对路径。
- 对项目中难以逆转、影响范围大的技术选择记录 ADR；小型局部实现不滥用 ADR。

## 9. 安全、隐私、成本与外部副作用

- 绝不提交 API key、token、cookie、私钥、真实 `.env`、个人信息或敏感提示词/数据。发现泄漏时停止扩散并立即报告。
- 未经用户明确授权，不执行付费模型批量调用、部署、发布、删除远端资源、写入外部系统或其他不可逆操作。
- `CLIProxyAPI`、`Xray`、`Caddy` 等现有宿主机服务属于基础设施，不是 AI Lab 实验对象；除非用户明确要求，不得修改、重启、升级、重配或删除。
- AI Ops/SRE 实验默认使用隔离的 **Docker Test Lab**；绝不将重要宿主机服务作为故障注入目标。
- 删除大量文件、重置状态、迁移数据或执行破坏性命令前必须请求确认；优先 dry-run、备份和可回滚方案。
- 日志和示例数据需脱敏。引入数据集、模型、代码或媒体时检查许可证和可再分发性。
- 不提交虚拟环境、依赖目录、缓存、模型权重、大型数据、临时日志和构建产物。

## 10. 完成定义（Definition of Done）

任务只有在以下条件满足后才算完成：

- 内容放在正确目录，范围聚焦，没有夹带无关变更。
- 运行方式和关键设计可由 README 或代码直接理解，不依赖特定 Coding Agent 的会话上下文。
- 对作品项目，已明确真实用户问题、baseline、量化指标、失败案例和演示路径；教程 Demo 不得标记为完成作品。
- 学习模块或单日不得仅因代码已生成而标记完成；还必须完成相关验证或评测，并达到用户能够解释关键机制与结果的理解标准。对核心学习里程碑，适当时应先请用户确认理解。
- 已执行最相关的验证并记录结果；若未执行，明确写出未执行项和原因。
- 对应索引、状态、实验结论或项目文档已同步。
- 没有引入凭据、敏感数据、机器专属路径或不必要的大型产物。
- 最终汇报包含：变更摘要、验证命令与结果、已知限制、可选下一步；不得把“未运行”描述成“已通过”。
