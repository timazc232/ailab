# 2026-09-09 Daily Log（Day 9 — M1.9 Retry / Timeout / Error Recovery + Basic Tracing）

## 今日唯一重点

分类重试、deadline、幂等性判断与最小 trace：可靠性不能靠无限重试。

## 计划输出

- [x] 概念步：transient vs permanent、timeout vs deadline、backoff、幂等性、trace（用户复述确认）
- [x] retry policy + 虚拟时钟 + trace recorder + runner（n1–n8）
- [x] 故障注入矩阵实测（8/8）
- [x] M1.9 Evaluation Cases 归档（累计 55）；用户解释确认，M1.9 Completed（Phase 1 全部完成）

## 实际完成

- 实现：分类 RetryPolicy、deadline 检查（每次尝试前）、指数 backoff（虚拟时钟）、幂等性门控、显式 fallback、append-only TraceRecorder。
- 实测：n1–n8 全部通过；n2 恰好 3 次尝试；n5 deadline 后零额外尝试；n6 非幂等不自动重试；n8 trace 重建与实际一致。

## 证据

- 代码/文档：`playground/agent-lab/m1-9-reliability/`；`90-days/daily/day-09-reliability-tracing-plan.md`
- 执行命令：`python3 run_reliability_scenarios.py` → `PASS: 8 cases`
- 测试/指标：重试上限、deadline、幂等门控、fallback、trace 重建全部命中预测；Evaluation 累计 55 条

## 学到的内容

### 已验证

- 429/超时 transient 按上限重试；401 permanent 立即失败不重试。
- deadline 在每次发起新尝试前检查；到期后零额外尝试（n5：clock=300 ≥ deadline=150，无第 3 次）。
- 非幂等副作用工具失败转 manual_review，不自动重试。
- fallback 必须显式声明；trace JSONL 可独立重建运行结论（seq 严格递增、run_id 一致）。
- 用户复述确认（2026-09-09）：原始尝试与重试计数；deadline 在发起新尝试前检查；transient 只是必要条件（次数/预算/幂等/业务策略）；attempt 字段对 trace 消歧的必要性。

### 推断或待验证

- 虚拟时钟末实测真实网络时延分布；backoff 无 jitter；幂等键机制未实现（只做了门控）；trace 未与 M1.7 loop 集成。

## 阻塞与失败

- 首次运行 n8 打印格式化 bug（outcome=None）；修复后 8/8。

## 下一步

Day 9 关闭：M1.9 Completed（用户复述确认，2026-09-09）；Phase 1 全部九个 Module 完成。下次会话：Day 30 Checkpoint（复盘 + baseline 汇总 + Phase 2 前置确认）。
